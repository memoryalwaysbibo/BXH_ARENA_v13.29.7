# BXH ARENA v13.29.7 — BUG 010 優先修復包

本次基於 v13.29.6，僅優先修復賽事紀錄讀取穩定性。其餘原排定 .7 功能仍待開發，不包含相機修復、測試帳號對標管理員、帳號批次管理、報名倒數及封存按鈕灰化。

## 已修改

- 紀錄讀取失敗後停止自動重試，顯示原因與手動重新同步入口。
- 一次完整同步最多等待 30 秒；逾時停止等待，可手動重試。底層網路請求未必能取消，但遲到結果不得覆寫資料或接續讀取更多紀錄。
- 離開紀錄頁及切換登入身分後作廢舊同步，防止舊請求覆蓋新頁面。
- 同一頁反覆重繪或點選目前分頁，不重複發出同步請求。
- 新增返回賽事管理按鈕；載入中明示仍可切換分頁。
- 權限拒絕、網路失敗、逾時、部分成功與真正無紀錄分開呈現。
- 雲端取得的歷史紀錄改用當次登入的記憶體快取，不再在同步流程另寫本機快照，避免遲到回應繼續寫入。既有本機紀錄、備份匯出與雲端資料均不刪除；重新開啟網頁需重新讀取這些雲端紀錄，離線可用性因此受限。
- 既有角色與 Firestore 權限不放寬；權限不足將如實顯示，這版不修正帳號權限設定。

## 本機驗證

相同的權限拒絕模擬，.6 在測試截停前發出 4 次請求、重繪 9 次；.7 為 1 次請求、3 次重繪。這是本機程式重現，不是從使用者 iPhone 取得的執行紀錄，尚不能證明錄影只有這一個原因。

通過六支測試：

```bash
node test_history.cjs
node test_behavior.cjs
node test_publish.cjs
node test_accounts.cjs
node test_auth.cjs
node test_courts.cjs
```

新測試涵蓋實際 renderHistory 排程、permission-denied、離線錯誤、手動重試、逾時及遲到回應、離頁、身分切換、部分成功、空紀錄、重複排程與計時器清理。既有五支測試涵蓋稱號簽到、發布與返回、帳號、登入及裁判排程。測試使用本機 Node 與模擬資料；未進行新一輪 Firestore emulator、線上資料、Safari 或 iPhone 實機驗收。

## 如何更新

1. 備份目前網站的 index.html。
2. 將本包 index.html 覆蓋 GitHub Pages 對應網站的 index.html。
3. 網站發布完成後重新整理，確認版本 v13.29.7。
4. 本包 Functions 程式及 Rules 與 .6 交付內容相同，仍為 13.29.4；本次不需要重新部署 Firebase，也不要建立新專案或清除資料。
5. 需要回復時換回備份 index.html；本次沒有資料遷移。

## iPhone 驗收

- 使用回報 BUG 010 的原帳號及原瀏覽器開啟賽事紀錄，記下實際瀏覽器名稱及版本。
- 同步完成或錯誤後，測試橫列切至其他分頁及返回按鈕。
- 切為離線後重新同步，確認出現錯誤或約 30 秒後逾時，頁面不反覆跳動；恢复網路再手動同步。
- 讀取尚未完成時切換分頁，確認舊請求不把頁面拉回；重新進入應可重新讀取。
- 若仍有卡住，回傳操作錄影、發生時間與顯示的錯誤文字，勿公開密碼或登入 token。

此包已完成本機修正與測試，尚未代為部署正式網站。原 .7 其他功能尚未完成，不可依本版本號假定已具備管理員測試權限。
